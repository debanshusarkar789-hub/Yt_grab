services:
  - type: web
    name: yt-grab
    env: node
    buildCommand: |
      cd frontend && npm install && npm run build
      cd ../backend && npm install
    startCommand: bash start.sh
    envVars:
      - key: NODE_VERSION
        value: 20
